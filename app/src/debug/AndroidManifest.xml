<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!--
            Debug diagnostics remain available to engineering by explicit intent,
            but they must NEVER become a launcher entry in installable test APKs.
            ProductionActivity is the sole MAIN/LAUNCHER activity.
        -->
        <activity
            android:name=".FilterDiagnosticsActivity"
            android:exported="false"
            android:label="AERMOTUS R8 Diagnostics" />
    </application>
</manifest>
